name: Validate

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: "0 4 * * 1"
  workflow_dispatch:

jobs:
  validate:
    uses: MauroDruwel/ha-quality-gate/.github/workflows/validate.yml@main
